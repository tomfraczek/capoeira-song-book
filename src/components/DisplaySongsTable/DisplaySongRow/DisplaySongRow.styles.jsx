import styled from 'styled-components';

export const IconContainer = styled.span`
    svg {
        color: #0000008a;
        &:hover {
            cursor: pointer;
            color: #000;
        }
    }
`;
